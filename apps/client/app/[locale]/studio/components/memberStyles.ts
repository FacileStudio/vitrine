// what is left after the global chart: a chip is a `p` in a box, and a button is
// a `p` the browser will not let us write as one. Everything else these used to
// carry — face, weight, case, tracking, size — is now the `p` rule in globals.css
export const chipClass = "rounded-md px-[2vh] py-[1vh] bg-[#212121] text-white";

export const buttonClass = "rounded-md bg-white/5 px-4 py-3 font-bb-mono text-[0.75rem] tracking-tight font-medium uppercase text-white/80 transition-colors active:bg-white/15";
